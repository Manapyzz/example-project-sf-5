<h1>Liste des idées pour {{ theme.name }}</h1>

<ul>
    {% for idea in theme.ideas %}
        <li>{{ idea.name }}</li>
    {% endfor %}
</ul>

<a href="{{ path('create_idea', { themeId: theme.id }) }}">Ajouter une idée pour le thème {{ theme.name }}</a>